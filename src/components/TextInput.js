import { View, StyleSheet } from "react-native";
import { Text, TextInput as Input } from "react-native-paper";

export const TextInput = ({ error, errorText, secureTextEntry }) => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({

});